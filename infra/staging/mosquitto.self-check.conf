listener 1883 0.0.0.0
allow_anonymous true
persistence true
persistence_location /mosquitto/data/

# Staging worker self-check only. Do not expose this listener publicly.
# Production MQTT must use TLS, unique device credentials, and generated ACLs.
