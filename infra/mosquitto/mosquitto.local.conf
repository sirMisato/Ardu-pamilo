listener 1883 0.0.0.0
allow_anonymous true
persistence true
persistence_location /mosquitto/data/

# Local development only. Production must use TLS, unique credentials,
# anonymous disabled, and generated per-device ACLs.
